{
  "name": "@clairex/core",
  "description": "A class-based, explicitly-typed web framework for Bun. OOP-first, built-in validation, zero external dependencie",
  "module": "dist/index.js",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "1.0.0",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && bun build ./src/index.ts ./src/plugin/claire-loader.ts --outdir ./dist --target bun && bunx tsc -p tsconfig.build.json",
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "app": "bun run example/app.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@clairex/typescript-plugin": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ],
"exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./plugin": "./dist/plugin/claire-loader.js"
}

}